{
  "name": "frontend-zara",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c red,blue --kill-others-on-fail \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
